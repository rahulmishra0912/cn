after compiling by doing cc, run the program by using "sudo" as a prefix
well "sudo ./a.out"
and while the code is running you need to be connected to the internet and you have to open browser and search some
page in it, you'll see the packet details of it in "log.txt" that's generated after you run this program.
 